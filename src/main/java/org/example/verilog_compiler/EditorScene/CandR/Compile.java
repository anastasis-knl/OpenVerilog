package org.example.verilog_compiler.EditorScene.CandR;

public class Compile {

    // takes all saved files
    // compiles all saved files somehow
    // runs the files based on the runnable files ? ??? whwat is the runable file on verilog ? the main file
    // the main file stars calling components which are like runable files the test bench
    // each has it's own different output
}
